# Regex NFA to DOT

TODO

Regex grammar in LL(1):

```
re -> ...
```

This doesn't allow empty strings...

Passes:
* Tokenize
* Parse into an AST
* Convert AST to NFA
* Write NFA in DOT
